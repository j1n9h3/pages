---
title: 天空和大气的渲染 单散射实现
description: 参考 1993 年工作的大气渲染单次散射实现
---


<Card
  title="完整代码实现"
  href="https://github.com/j1n9h3/Alpha3D/blob/v0.1.6/shaders/sky_atmosphere_1993.frag"
>
  Alpha3D v0.1.6
</Card>

<ImageSequenceSlider
  images={[
    "sky_atmosphere/sky_atmosphere_1993_slider/photo-57.png",
    "sky_atmosphere/sky_atmosphere_1993_slider/photo-58.png",
    "sky_atmosphere/sky_atmosphere_1993_slider/photo-59.png",
    "sky_atmosphere/sky_atmosphere_1993_slider/photo-60.png",
    "sky_atmosphere/sky_atmosphere_1993_slider/photo-61.png",
    "sky_atmosphere/sky_atmosphere_1993_slider/photo-62.png",
    "sky_atmosphere/sky_atmosphere_1993_slider/photo-63.png",
    "sky_atmosphere/sky_atmosphere_1993_slider/photo-64.png",
    "sky_atmosphere/sky_atmosphere_1993_slider/photo-65.png",
    "sky_atmosphere/sky_atmosphere_1993_slider/photo-66.png",
    "sky_atmosphere/sky_atmosphere_1993_slider/photo-67.png",
    "sky_atmosphere/sky_atmosphere_1993_slider/photo-69.png",
    "sky_atmosphere/sky_atmosphere_1993_slider/photo-70.png",
    "sky_atmosphere/sky_atmosphere_1993_slider/photo-71.png",
    "sky_atmosphere/sky_atmosphere_1993_slider/photo-72.png",
    "sky_atmosphere/sky_atmosphere_1993_slider/photo-73.png",
    "sky_atmosphere/sky_atmosphere_1993_slider/photo-74.png",
    "sky_atmosphere/sky_atmosphere_1993_slider/photo-75.png",
    "sky_atmosphere/sky_atmosphere_1993_slider/photo-76.png",
    "sky_atmosphere/sky_atmosphere_1993_slider/photo-77.png",
    "sky_atmosphere/sky_atmosphere_1993_slider/photo-78.png",
  ]}
/>


参照论文 [Nishita 1993 - Display of The Earth Taking into Account Atmospheric Scattering](https://dl.acm.org/doi/abs/10.1145/166117.166140) 实现。

<OssImage
  src="sky_atmosphere/sky_atmosphere_1993.svg"
  alt="天空和大气的渲染"
  width={320}
  height={320}
/>

观察射线从观察点 $P_v$ 出发，在 $P_a$ 处进入大气层，并在 $P_b$ 处与地球表面相交。摄像机最终接收到的光，可以描述为路径 $P_aP_b$ 上各点产生的大气散射光 $I_v$ 的累积。太阳光到达路径上的某个大气采样点 $P$ 后，一部分光被散射到观察方向，并在从 $P$ 传播到观察点 $P_v$ 的过程中继续受到大气消光作用。

地球大气主要由氮气、氧气等气体分子，以及臭氧和气溶胶等成分组成。臭氧等物质会选择性地吸收特定波长的光；气体分子和气溶胶则会使光发生散射。气体分子的尺度远小于可见光波长，其散射通常使用瑞利散射描述；气溶胶颗粒的尺度与可见光波长接近，其散射通常使用米氏散射描述。

光的散射由散射系数和相位函数共同描述，光的吸收与整体衰减则由吸收系数以及比尔–朗伯定律描述。摄像机接收到的大气散射光，是观察路径上各采样点产生的散射贡献经过两段路径衰减后累积得到的结果。

## 光散射的描述

定义光的散射模型：输入波长为 $\lambda$ 的入射光强度，输出 $\theta$ 角度散射后的光强度。

$$
I(\lambda,\theta)=I_0(\lambda)\,K\rho\,\frac{F_R(\theta)}{\lambda^4}. \tag{1}
$$

其由 $K$、$\rho$ 和 $F_R(\theta)$ 描述。

- $K$ 与空气分子物理性质有关，为常数；
- $\rho$ 描述当前高度的相对空气密度，$\rho(h)=\exp\left(-\frac{h}{H_0}\right)$；
- $F_R(\theta)$ 表示散射相函数，描述散射的方向分布。

指数密度模型在 Shader 中可以直接写成：


其中返回值的两个分量分别保存瑞利粒子和米氏粒子的相对密度。后续只需乘以各自的散射系数，就能得到采样点处的散射强度。

## 光学深度

定义光沿整条路径传播时，沿途每一小段大气造成的消光作用之和。

考虑路径上的一小段 $ds$。该处的大气密度比例是 $\rho(s)$，标准大气下单位长度的消光系数是 $\beta(\lambda)$，这一小段产生的光学深度为 $dt=\beta(\lambda)\rho(s)\,ds$。

$$
t(S,\lambda)
=
\int_0^S
\beta(\lambda)\rho(s)\,ds
=
\frac{4\pi K}{\lambda^4}
\int_0^S\rho(s)\,ds.
\tag{2}
$$

连续积分在 GPU 上需要离散为有限个采样点。将长度为 `rayLength` 的路径均分为 $N$ 段，并使用中点法累积柱密度：


`columnDensity` 对应 $\int\rho(s)\,ds$，最后分别乘以 RGB 消光系数，得到式（2）中的光学深度 $\tau$。

## Beer–Lambert 衰减

光穿过介质时，光强会随着传播距离按指数形式减小

$$
I_{\mathrm{out}}=I_{\mathrm{in}}e^{-\tau}. \tag{3}
$$

其中 $\tau$ 表示光学深度，$e^{-\tau}$ 即透射率(transmittance)，表示剩余的光比例。


GLSL 的 `exp` 会逐分量计算，因此 RGB 三个波段可以同时应用 Beer–Lambert 衰减。

## 单次散射的路径积分

假设 $I_s(\lambda)$ 为平行光，因此 $\theta$ 为常数，描述 $P$ 点的光照强度 $I_p(\lambda)$ 如下：

$$
I_p(\lambda)=I_s(\lambda)\,K \rho\,\frac{F_R(\theta)}{\lambda^4}\exp\left[-t(PP_c,\lambda)\right]. \tag{4}
$$

其中，光学深度描述如下：

$$
t(PP_c,\lambda)=\int_{P_c}^{P}\beta(l,\lambda)\rho(l)\,dl. \tag{5}
$$


由 Beer–Lambert 可以得到 $P$ 点的光照强度：

$$
I_{pv}(\lambda)=I_p(\lambda)\exp\left[-t(PP_a,\lambda)\right]. \tag{6}
$$


将式（4）代入式（6），可以得到采样点 $P$ 对观察点 $P_v$ 的光强贡献：

$$
I_{pv}(\lambda)=I_s(\lambda)\,K\rho(P)\,\frac{F_R(\theta)}{\lambda^4}\exp\left[-t(PP_c,\lambda)-t(PP_a,\lambda)\right]. \tag{7}
$$

累计路径 $P_bP_a$ 上的每个采样点的光强贡献，进行积分：

$$
I_v(\lambda)=\int_{P_b}^{P_a}I_{pv}(P,\lambda)\,ds. \tag{8}
$$

将式（7）代入式（8），得到：

$$
I_v(\lambda)=I_s(\lambda)\frac{K F_R(\theta)}{\lambda^4}\int_{P_b}^{P_a}\rho(P)\exp\left[-t(PP_c,\lambda)-t(PP_a,\lambda)\right]\,ds. \tag{9}
$$

根据散射系数的定义：

$$
\beta_R(\lambda)=\frac{4\pi K}{\lambda^4}, \tag{10}
$$

再定义归一化相函数：

$$
p_R(\theta)=\frac{F_R(\theta)}{4\pi}, \tag{11}
$$

则有：

$$
\beta_R(\lambda)p_R(\theta)=\frac{K F_R(\theta)}{\lambda^4}. \tag{12}
$$

因此，式（9）可以改写为：

$$
I_v(\lambda)=I_s(\lambda)\beta_R(\lambda)p_R(\theta)\int_{P_b}^{P_a}\rho_R(P)\exp\left[-t(PP_c,\lambda)-t(PP_a,\lambda)\right]\,ds. \tag{13}
$$

代入光学深度公式 (5)，得到：

$$
I_v(\lambda)
=
I_s(\lambda)\beta_R(\lambda)p_R(\theta)
\int_{P_b}^{P_a}
\rho_R(P)
\exp\left[
-\int_{P}^{P_c}
\beta(l_1,\lambda)\rho(l_1)\,\mathrm{d}l_1
-\int_{P}^{P_a}
\beta(l_2,\lambda)\rho(l_2)\,\mathrm{d}l_2
\right]
\,\mathrm{d}s.
\tag{14}
$$

式（14）对应两层积分：外层沿观察射线寻找产生散射的采样点，内层计算太阳到采样点以及采样点到观察者两段路径的透射率。离散后的核心结构如下：


其中 `sunTransmittance` 对应 $\exp[-t(PP_c)]$，`viewTransmittance` 对应 $\exp[-t(PP_a)]$，`localScattering * stepLength` 则是采样点 $P$ 对外层积分的一小段贡献。



## 大气密度分布

实现分别计算瑞利散射粒子、米氏散射粒子和吸收介质的相对密度。设采样点海拔高度为 $h$，瑞利和米氏密度都使用指数大气模型：

$$
\rho_R(h)=\exp\left(-\frac{h}{H_R}\right),
\qquad
\rho_M(h)=\exp\left(-\frac{h}{H_M}\right). \tag{12}
$$

$H_R$ 和 $H_M$ 分别是瑞利与米氏密度的标高。标高越大，密度随高度下降得越慢。气溶胶通常集中在近地面，因此米氏标高通常明显小于瑞利标高。

吸收介质使用以 $h_A$ 为中心、由 $H_A$ 控制宽度的钟形分布：

$$
o(h)=\frac{h_A-h}{H_A},
\qquad
\rho_A(h)=\frac{\rho_R(h)}{o(h)^2+1}. \tag{13}
$$

分母让密度在吸收层中心附近较大，并随偏离中心的距离平滑下降；额外乘以瑞利密度，使高空部分继续随整体大气密度衰减。这是适合实时渲染的简化分布，而不是对真实臭氧浓度剖面的严格拟合。


## 可见光光谱


可见光的波长大约位于 $380\text{–}780 \, \mathrm{nm}$。下图描述了波长从短到长，颜色的变化：


<OssImage
  src="/sky_atmosphere/visible_spectrum.png"
  alt="天空和大气的渲染"
  width={600}
  height={72}
/>


整体关系可以表示为：

$$
\text{短波长}
\rightarrow
\text{紫、蓝、绿、黄、橙、红}
\rightarrow
\text{长波长}
$$

## 瑞利散射

瑞利散射（Rayleigh）描述空气分子对光的散射，主要决定天空、大气边缘以及远距离景物的基础颜色。其散射强度近似与波长的四次方成反比：

$$
\beta_R(\lambda)\propto\frac{1}{\lambda^4}
$$

因此，波长较短的蓝光比波长较长的红光更容易被散射。


白天，天空蓝光被更多地散射到观察者方向，使天空呈蓝色。

<ImageComparison
  before="/sky_atmosphere/sky_atmosphere_1993_rayleigh-wo-1.png"
  after="/sky_atmosphere/sky_atmosphere_1993_rayleigh-w-1.png"
  beforeAlt="None"
  afterAlt="Rayleigh"
  beforeLabel="None"
  afterLabel="Rayleigh"
/>

日出和日落时，太阳光经过更长的大气路径，蓝光被大量散射出去，直射太阳光偏橙红色。

<ImageComparison
  before="/sky_atmosphere/sky_atmosphere_1993_rayleigh-wo-2.png"
  after="/sky_atmosphere/sky_atmosphere_1993_rayleigh-w-2.png"
  beforeAlt="None"
  afterAlt="Rayleigh"
  beforeLabel="None"
  afterLabel="Rayleigh"
/>

瑞利散射相函数通常写为：

$$
p_R(\theta)=\frac{3}{16\pi}\left(1+\cos^2\theta\right)
$$

它表示散射光在前向和后向较强，在侧向相对较弱，并且前后方向基本对称。


实现时直接传入 $\cos\theta$，避免在每个采样点调用 `acos`。由于太阳光被视为平行光，同一条观察射线上的 `cosTheta` 保持不变，因此相函数可以移到积分循环外计算一次。

## 米氏散射

米氏散射（Mie）用于描述气溶胶、灰尘、水滴等较大颗粒对光的散射。米氏散射通常具有明显的前向散射特征，即光更容易沿原来的传播方向被散射。因此，朝太阳方向观察时会看到更强的亮度和光晕。

通常使用 Henyey–Greenstein 相函数近似其方向分布：

$$
p_M(\theta)
=
\frac{1-g^2}
{4\pi\left(1+g^2-2g\cos\theta\right)^{3/2}}
$$


`max` 用来防止 $g$ 和 $\cos\theta$ 接近 1 时分母过小。这个方向正是米氏前向散射最集中的区域，也对应太阳周围明亮的光晕。

其中 $g$ 是米氏各向异性系数：

| $g$ | 散射方向 |
|:---:|:---:|
| $= 0$ | 各向同性散射 |
| $> 0$ | 前向散射 |
| $< 0$ | 后向散射 |
| $\rightarrow 1$ | 高度集中的前向散射 |

实现中 $g=0.7$，表示具有比较明显的前向散射，所以太阳周围会形成较集中的亮光晕。

<ImageComparison
  before="/sky_atmosphere/sky_atmosphere_1993_mie-wo-1.png"
  after="/sky_atmosphere/sky_atmosphere_1993_mie-w-1.png"
  beforeAlt="Rayleigh"
  afterAlt="Rayleigh + Mie"
  beforeLabel="Rayleigh"
  afterLabel="Rayleigh + Mie"
/>

三个颜色通道受到近似相同强度的散射，Mie 散射通常表现为白色或灰白色，而不像 Rayleigh 散射那样明显偏蓝。

## 臭氧吸收

臭氧层选择性地削弱部分可见光，臭氧对绿光吸收最强，对红光次之，对蓝光最弱（$\beta_G \gt \beta_R \gt \beta_B$）。

实现使用一个以 `absorptionHeight` 为中心的平滑密度层近似臭氧分布：


计算消光时，臭氧密度只乘以吸收系数，不会像瑞利或米氏粒子一样产生朝观察方向的散射贡献：



日出、日落时，光在大气中的传播路径变长，臭氧对绿色和黄绿色成分的选择性吸收更加明显，从而改变天空的色彩平衡，使部分区域呈现更明显的蓝紫色或粉紫色过渡。

<ImageComparison
  before="/sky_atmosphere/sky_atmosphere_1993_absorption-wo-2.png"
  after="/sky_atmosphere/sky_atmosphere_1993_absorption-w-2.png"
  beforeAlt="Rayleigh + Mie"
  afterAlt="Rayleigh + Mie + Absorption"
  beforeLabel="Rayleigh + Mie"
  afterLabel="Rayleigh + Mie + Absorption"
/>


从太空观察时，大气分子产生的瑞利散射使行星边缘呈现蓝色光环，臭氧吸收会进一步削弱部分红光和绿光，从而改变并强化大气边缘的颜色表现。

<ImageComparison
  before="/sky_atmosphere/sky_atmosphere_1993_absorption-wo-1.png"
  after="/sky_atmosphere/sky_atmosphere_1993_absorption-w-1.png"
  beforeAlt="Rayleigh + Mie"
  afterAlt="Rayleigh + Mie + Absorption"
  beforeLabel="Rayleigh + Mie"
  afterLabel="Rayleigh + Mie + Absorption"
/>


## 附：预设的超参数

行星与大气几何参数定义行星及其大气层的球形几何范围：

| 超参数 | 值 | 说明 |
|---|:---:|---|
| 行星半径 | $6,371,000.0 \, \mathrm{m}$ | 行星表面的球体半径，此处采用近似地球平均半径 |
| 大气层半径 | $6,471,000.0 \, \mathrm{m}$ | 大气层外边界半径，对应约 100 km 的大气层厚度 |

大气光学参数描述不同大气成分对光线产生的散射和吸收强度：

| 超参数 | R 通道 | G 通道 | B 通道 | 说明 |
|---|:---:|:---:|:---:|---|
| 瑞利散射系数 | $5.5 \times 10^{-6}\,\mathrm{m}^{-1}$ | $13.0 \times 10^{-6}\,\mathrm{m}^{-1}$ | $22.4 \times 10^{-6}\,\mathrm{m}^{-1}$ | $\beta_{R,B}>\beta_{R,G}>\beta_{R,R}$，蓝光的散射最强，红光的散射最弱 |
| 米氏散射系数 | $21.0 \times 10^{-6}\,\mathrm{m}^{-1}$ | $21.0 \times 10^{-6}\,\mathrm{m}^{-1}$ | $21.0 \times 10^{-6}\,\mathrm{m}^{-1}$ |  $\beta_{M,R}=\beta_{M,G}=\beta_{M,B}$，三个通道的散射强度相同，近似不改变光的颜色，主要影响太阳光晕、雾霾和地平线亮度 |
| 吸收系数 | $2.04 \times 10^{-5}\,\mathrm{m}^{-1}$ | $4.97 \times 10^{-5}\,\mathrm{m}^{-1}$ | $1.95 \times 10^{-6}\,\mathrm{m}^{-1}$ |  $\beta_{A,G}>\beta_{A,R}>\beta_{A,B}$，绿光吸收最强，蓝光吸收最弱 |

大气密度分布参数控制空气分子、气溶胶及吸收介质的密度如何随海拔高度变化：

| 超参数 | 值 | 说明 |
|---|:---:|---|
| 瑞利散射标高 | $8,000.0  \, \mathrm{m}$ | 空气分子密度随高度指数衰减的尺度高度 |
| 米氏散射标高 | $1,200.0  \, \mathrm{m}$ | 气溶胶密度随高度指数衰减的尺度高度 |
| 吸收层中心高度 | $30,000.0  \, \mathrm{m}$ | 吸收介质密度最大的高度，通常对应臭氧层附近 |
| 吸收层衰减高度 | $4,000.0  \, \mathrm{m}$ | 吸收介质密度偏离中心高度后的衰减尺度 |

散射方向参数描述光被大气微粒散射后的方向分布：

| 超参数 | 值 | 说明 |
|---|:---:|---|
| 米氏各向异性系数 | $0.7$ | 控制米氏散射的方向性；值越接近 1，前向散射越强，太阳周围的光晕越集中 |
